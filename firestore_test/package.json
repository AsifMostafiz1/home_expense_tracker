{
  "name": "meal-tracker-firestore-rules-test",
  "private": true,
  "type": "module",
  "description": "Emulator tests for ../firestore.rules. Not part of the Flutter build.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-rules-check --config ../firebase.json \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.0.0"
  }
}
